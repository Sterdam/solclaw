# SolClaw Environment Variables

# Server private key (JSON array format)
# This wallet will be used by the API to sign transactions
# Generate with: solana-keygen new --outfile key.json
# Then copy the contents here
SERVER_PRIVATE_KEY=[1,2,3,...]

# RPC URL (optional, defaults to devnet)
RPC_URL=https://api.devnet.solana.com

# API Port (optional, defaults to 3000)
PORT=3000
